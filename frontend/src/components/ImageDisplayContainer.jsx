import React from "react";

const ImageDisplayContainer = () => {
  return (
    <div className="w-full h-screen fixed z-10 bg-red-200">
      ImageDisplayContainer
    </div>
  );
};

export default ImageDisplayContainer;
